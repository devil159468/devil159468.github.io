# 埋点SDK

## 项目初始化
```bash
# 初始化 package.json
npm init -y

# 全局安装 typescript
npm i typescript -g

# 初始化 typescript
tsc --init

# 安装rollup
npm install rollup -D
npm install rollup-plugin-dts -D
npm install rollup-plugin-typescript2 -D

```
